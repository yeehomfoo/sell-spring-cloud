spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: git@gitee.com:yeehomfoo/spring-cloud-config-private.git
          basedir: ./basedir
management:
  endpoints:
    web:
      exposure:
        include: '*'